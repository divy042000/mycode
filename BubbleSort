// Online C++ compiler to run C++ program online
#include <bits/stdc++.h>
using namespace std;

int main() {
   vector<int> v;
   v={4,9,10,78,56};
   int n=v.size();
   for(int i=0;i<n;i++)
   {
    for(int i=1;i<n-1;i++)
    {
        if(v[i]>v[i+1])
        {
            swap(v[i],v[i+1]);
        }
    }
   }
   for(auto i:v)
   {
       cout<<i<<" ";
   }
    return 0;
}
